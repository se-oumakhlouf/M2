package fr.uge.poo.paint.ex7;

public interface RenderableCanvas extends Canvas {

	void render();
	// La méthode render doit être dans l'interface Canvas
	// Sinon on a une différence de comportement entre les librairies
	
}
